pipeline{
    agent any
    tools {
        terraform 'terraform'
    }
    
    stages {
        /*stage('Build Infrastructure') {
            steps {
                git branch: 'main', credentialsId: 'github-ssh-key', url: 'git@github.com:unicum0212/petclinic-devops-deploy.git'
                dir('terraform/env_1'){
                    sh 'terraform init -upgrade'
                    sh 'terraform apply -auto-approve -no-color'
                }
            }
        }*/
        stage('Build image') {
            steps {
                sh 'docker build flawwwless/dynamic-inventory-ansible:latest .'
            }
        }
        stage('Push Image') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'dockerHub', passwordVariable: 'dockerHubPassword', usernameVariable: 'dockerHubUser')]) {
                    sh "docker login -u ${env.dockerHubUser} -p ${env.dockerHubPassword}"
                    sh 'docker push flawwwless/dynamic-inventory-ansible:latest'
                }
            }
        }
    }
}